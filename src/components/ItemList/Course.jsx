import React from 'react'
import styles from "./Course.module.css"

export function Course(props){
    <div>
        <p>{props.name}</p>
        <p>{props.semester}</p>
        <p>{props.ch}</p>
    </div>
}